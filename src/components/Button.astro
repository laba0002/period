---
const {variant="primary", size="medium"} = Astro.props;

const buttonStyling = " border-blue-900 rounded-border_radius-3xl border-md tracking-wide font-bold transition ease-in-out delay-100"; 

const variantClasses = {
"textlink": "underline",

"primary": " border-red-500 bg-red-50 text-red-500 shadow-dropred hover:shadow-innerred",

"secondary": "border-blue-700 bg-blue-50 text-blue-700 hover:text-gray-200 hover:border-blue-900 shadow-dropblue hover:shadow-innerblue",

};

const sizeClasses = {

"large": "text-mediumDesk py-s px-lg",

"medium": "text-smallMediumDesk py-xs px-lg",

"small": "text-baseDesk py-[14px] px-m",

};
---
<div>
    <button class:list={[buttonStyling, variantClasses[variant], sizeClasses[size]]}><slot /></button>
</div>


